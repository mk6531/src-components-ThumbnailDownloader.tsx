/* Design System */
:root{
  --color-primary:#32b8c6;
  --color-primary-hover:#2da6b2;
  --color-bg:#fcfcf9;
  --color-surface:#fffffe;
  --color-text:#134252;
  --color-text-secondary:#626c71;
  --color-border:rgba(94,82,64,.2);
  --color-success:#21808d;
  --color-error:#c0152f;

  --radius-sm:6px; --radius:10px; --radius-lg:14px;
  --shadow-sm:0 1px 2px rgba(0,0,0,.04);
  --shadow-md:0 6px 16px rgba(0,0,0,.06);

  --space-8:8px; --space-12:12px; --space-16:16px; --space-20:20px; --space-24:24px; --space-32:32px;

  --fz-12:12px; --fz-14:14px; --fz-16:16px; --fz-18:18px; --fz-20:20px; --fz-24:24px; --fz-30:30px;
}

@media (prefers-color-scheme:dark){
  :root{
    --color-bg:#1f2121;
    --color-surface:#262828;
    --color-text:#f5f5f5;
    --color-text-secondary:rgba(167,169,169,.7);
    --color-border:rgba(167,169,169,.22);
    --shadow-sm:0 1px 2px rgba(0,0,0,.28);
    --shadow-md:0 8px 24px rgba(0,0,0,.28);
  }
}

/* Base */
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0; font-family:-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Inter,system-ui,Arial,sans-serif;
  font-size:var(--fz-14); line-height:1.6; color:var(--color-text); background:var(--color-bg);
  -webkit-font-smoothing:antialiased; -moz-osx-font-smoothing:grayscale;
}

/* Header */
header{position:sticky; top:0; z-index:10; background:var(--color-surface); border-bottom:1px solid var(--color-border); box-shadow:var(--shadow-sm)}
nav{max-width:1200px; margin:0 auto; padding:var(--space-16) var(--space-20); display:flex; justify-content:space-between; align-items:center}
.logo{color:var(--color-primary); font-weight:700; text-decoration:none; font-size:var(--fz-18)}
.nav-links{list-style:none; display:flex; gap:var(--space-20); margin:0; padding:0}
.nav-links a{color:var(--color-text); text-decoration:none; padding:8px 10px; border-radius:8px; transition:.15s}
.nav-links a:hover{background:rgba(50,184,198,.08); color:var(--color-primary)}
@media (max-width:760px){.nav-links{display:none}}

/* Main */
main{max-width:1200px; margin:0 auto; padding:var(--space-32) var(--space-20)}

/* Hero */
.hero{text-align:center; padding:var(--space-32) 0 var(--space-24)}
.hero h1{font-size:clamp(22px,3.2vw,32px); margin:0 0 var(--space-12)}
.hero-subtitle{max-width:900px; margin:0 auto; color:var(--color-text-secondary); font-size:var(--fz-16)}
.browser-badges{margin-top:16px; display:flex; gap:10px; flex-wrap:wrap; justify-content:center}
.badge{display:inline-flex; align-items:center; gap:6px; padding:6px 10px; border-radius:999px; background:rgba(50,184,198,.08); border:1px solid var(--color-border); font-size:var(--fz-12)}
.badge::before{content:"✓"; color:var(--color-success); font-weight:800}

/* Sections */
.section-title{text-align:center; font-size:var(--fz-24); margin:var(--space-32) 0 var(--space-20)}

/* Converter */
.converter-card{background:var(--color-surface); border:1px solid var(--color-border); border-radius:var(--radius-lg); padding:var(--space-24); box-shadow:var(--shadow-md)}
.upload-area{border:2px dashed var(--color-border); border-radius:var(--radius-lg); padding:28px; text-align:center; cursor:pointer; transition:.2s; user-select:none}
.upload-area:focus{outline:3px solid rgba(50,184,198,.35); outline-offset:4px}
.upload-area.drag-over{border-color:var(--color-primary); background:rgba(50,184,198,.06)}
.upload-icon{font-size:40px; margin-bottom:8px}
.upload-text{font-size:var(--fz-16)}
.upload-subtext{font-size:var(--fz-12); color:var(--color-text-secondary)}
.file-input{display:none}

/* Settings */
.settings-panel{margin-top:16px; background:rgba(50,184,198,.06); border:1px dashed var(--color-border); border-radius:12px; padding:12px}
.settings-grid{display:grid; grid-template-columns:repeat(3,minmax(200px,1fr)); gap:12px}
@media (max-width:900px){.settings-grid{grid-template-columns:1fr}}
.setting-group{display:flex; flex-direction:column; gap:6px}
.setting-label{font-size:var(--fz-12); color:var(--color-text-secondary)}
.setting-input{padding:10px 12px; border:1px solid var(--color-border); border-radius:8px; background:var(--color-surface); color:var(--color-text)}
.quality-value{color:var(--color-primary); font-weight:700}

/* Stats */
.stats-panel{margin-top:16px; display:grid; grid-template-columns:repeat(4,minmax(160px,1fr)); gap:12px}
@media (max-width:760px){.stats-panel{grid-template-columns:repeat(2,1fr)}}
.stat-card{background:rgba(50,184,198,.06); border:1px solid var(--color-border); border-radius:12px; padding:14px; text-align:center}
.stat-label{font-size:var(--fz-12); color:var(--color-text-secondary)}
.stat-value{font-size:var(--fz-20); font-weight:800; color:var(--color-primary)}

/* File list */
.files-container{margin-top:16px}
.file-card{border:1px solid var(--color-border); background:var(--color-surface); border-radius:12px; padding:14px; margin-bottom:12px}
.file-header{display:flex; justify-content:space-between; align-items:center; gap:10px}
.file-name{font-weight:600}
.file-size{font-size:var(--fz-12); color:var(--color-text-secondary)}
.file-progress{height:6px; border-radius:999px; background:rgba(0,0,0,.08); overflow:hidden; margin:10px 0}
.progress-bar{height:100%; width:0%; background:linear-gradient(90deg,var(--color-primary),#41c8d6); transition:width .3s ease}
.file-actions{display:flex; gap:8px; flex-wrap:wrap}

/* Buttons */
.btn{display:inline-flex; align-items:center; justify-content:center; gap:8px; padding:10px 14px; border-radius:8px; border:1px solid transparent; font-weight:600; cursor:pointer; transition:.2s}
.btn-primary{background:var(--color-primary); color:#fff}
.btn-primary:hover{background:var(--color-primary-hover)}
.btn-secondary{background:rgba(0,0,0,.04); color:var(--color-text); border-color:var(--color-border)}
.btn-secondary:hover{background:rgba(0,0,0,.08)}
.btn-small{padding:8px 10px; font-size:var(--fz-12)}
.btn:disabled{opacity:.55; cursor:not-allowed}
.bulk-actions{display:flex; gap:10px; justify-content:center; margin-top:16px; flex-wrap:wrap}

/* Features */
.features-grid{display:grid; gap:16px; grid-template-columns:repeat(3,minmax(240px,1fr))}
@media (max-width:980px){.features-grid{grid-template-columns:repeat(2,1fr)}}
@media (max-width:640px){.features-grid{grid-template-columns:1fr}}
.feature-card{background:var(--color-surface); border:1px solid var(--color-border); border-radius:14px; padding:16px; box-shadow:var(--shadow-sm)}
.feature-icon{font-size:28px}

/* FAQ */
.faq-container{max-width:820px; margin:0 auto}
.faq-item{border:1px solid var(--color-border); border-radius:12px; background:var(--color-surface); margin-bottom:12px; overflow:hidden}
.faq-question{padding:14px 16px; display:flex; align-items:center; justify-content:space-between; cursor:pointer}
.faq-question:hover{background:rgba(0,0,0,.04)}
.faq-toggle{transition:transform .2s}
.faq-item.active .faq-toggle{transform:rotate(180deg)}
.faq-answer{max-height:0; overflow:hidden; transition:max-height .25s ease}
.faq-answer-content{padding:0 16px 16px; color:var(--color-text-secondary)}
.faq-item.active .faq-answer{max-height:400px}

/* Footer */
footer{background:var(--color-surface); border-top:1px solid var(--color-border); margin-top:var(--space-32)}
.footer-content{max-width:1200px; margin:0 auto; padding:var(--space-32) var(--space-20); display:grid; gap:20px; grid-template-columns:repeat(4,minmax(220px,1fr))}
@media (max-width:980px){.footer-content{grid-template-columns:repeat(2,1fr)}}
@media (max-width:560px){.footer-content{grid-template-columns:1fr}}
.footer-links{list-style:none; margin:0; padding:0; display:grid; gap:8px}
.footer-links a{text-decoration:none; color:var(--color-text-secondary)}
.footer-links a:hover{color:var(--color-primary)}
.footer-bottom{max-width:1200px; margin:0 auto; padding:16px 20px; border-top:1px dashed var(--color-border); color:var(--color-text-secondary); text-align:center; font-size:var(--fz-12)}